pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'ls -l'
                sh 'cp ../../results.xml results.xml'
            }
	
        }
        post { 
            success { 
                junit 'results.xml'
            }
        }
    }
}
